apiVersion: v1
kind: Service
metadata:
  name: {{ include "chaincommand.fullname" . }}
  labels:
    {{- include "chaincommand.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "chaincommand.selectorLabels" . | nindent 4 }}
